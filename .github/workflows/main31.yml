name: spider31

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'

permissions:
  contents: write

jobs:
  poll-bms:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Python Dependencies
        run: |
          pip install requests curl-cffi

      - name: Configure Git User
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global pull.rebase true

      - name: Install and Setup Cloudflare WARP
        run: |
          # Add Cloudflare GPG key and repo
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg

          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list

          # Install WARP
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp

          # Configure WARP
          warp-cli --accept-tos registration new
          warp-cli --accept-tos mode proxy
          warp-cli --accept-tos connect

          sleep 5
          warp-cli --accept-tos status

          # Verify WARP
          curl -x socks5://127.0.0.1:40000 https://1.1.1.1/cdn-cgi/trace | grep warp || echo "WARP Test Failed, but continuing..."

      - name: Stop after 2 August 2026 (IST)
        shell: bash
        run: |
          END_DATE="2026-07-31"
          TODAY=$(TZ=Asia/Kolkata date +%F)

          echo "Today (IST): $TODAY"
          echo "End Date (IST): $END_DATE"

          if [[ "$TODAY" > "$END_DATE" ]]; then
            echo "31 july 2026. Stopping workflow."
            exit 0
          fi

      - name: Run Polling Script
        run: |
          python -u smbed_acdc_31072026.py
